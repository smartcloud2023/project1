name: Basic Gitaction

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  greet:
    name: Greet Smart
    runs-on: ubuntu-latest
    steps:
      - name: Check hostname
        run: |
          hostnamectl
          mkdir smartwork
          ls -la
          
  cook:
    name: cook rice
    runs-on: ubuntu-latest
    needs: greet
    steps:
      - name: check user info
        run: |
          cat /etc/passwd

  Clean:
    name: clean house
    runs-on: ubuntu-latest
    needs: cook
    steps:
      - name: check home file
        run: |
          ls -la /home

  
    
